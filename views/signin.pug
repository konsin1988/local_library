extends layout 

block content
    body 
        .sign
            +Previous_page_arrow("white")
            +Signup_signin("Sign in")

            +Input-bx("email", "email")
            +Input-bx("password", "password", "true")
            +Submit_btn("Sign in", "signin-btn")
            +Account_quest("Are you a new member? ", "SIGN UP")

        +Modal_window("Log In Successful", "Please wait a moment, we are preparing for you.")

    script(src="/scripts/lock_pass.js")